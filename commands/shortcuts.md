---
description: Install or remove the unprefixed /speaker-* commands (install, uninstall, status)
allowed-tools: Bash(bash:*)
---

```
!`SPK=""; for c in "$CLAUDE_PLUGIN_ROOT/bin/speak" "$HOME/claude-speaker/bin/speak" $(find "$HOME/.claude/plugins/cache" -maxdepth 5 -path "*speaker*/bin/speak" 2>/dev/null | sort -r); do [ -x "$c" ] && SPK="$c" && break; done; [ -n "$SPK" ] || { echo "claude-speaker not found"; exit 1; }; "$SPK" shortcuts $ARGUMENTS`
```

Report what happened in one line. After an install, say the shortcuts only appear
in the command list once Claude Code reloads the personal commands, and that both
forms then work: `/speaker-on` and `/speaker:on`. If any file was left untouched,
name it and say it was not generated by this plugin, so it was not overwritten.
